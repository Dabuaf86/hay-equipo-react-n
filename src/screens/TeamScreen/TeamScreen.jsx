import { Text, View } from 'react-native';
import style from '../Styles';

const TeamScreen = () => {
	return (
		<View style={style.container}>
			<Text style={style.title}>TeamScreen</Text>
		</View>
	);
};

export default TeamScreen;
